info:
  name: ToggleCustomerBlockStatus
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:8081/dripdoggy/api/admin/customers/1/toggle-block
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
