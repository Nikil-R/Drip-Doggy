info:
  name: Send-OTP
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8081/api/auth/send-otp
  body:
    type: json
    data: |-
      {
        "identifier": "annamalajeshwanth@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
