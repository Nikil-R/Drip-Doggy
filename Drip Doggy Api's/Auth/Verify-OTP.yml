info:
  name: Verify-OTP
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8081/api/auth/verify-otp
  body:
    type: json
    data: |-
      {
        "identifier": "annamalajeshwanth@gmail.com",
        "otpCode": "892387"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
