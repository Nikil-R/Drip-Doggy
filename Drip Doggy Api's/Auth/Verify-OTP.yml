info:
  name: Verify-OTP
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8081/dripdoggy/api/auth/verify-otp
  body:
    type: json
    data: |-
      {
        "identifier":"vi@gmail.com",
        "otpCode": "385343"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
